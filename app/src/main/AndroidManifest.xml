<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stl.story">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Story_App_HW_LAMA_SANJE"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Profession" />
        <activity android:name=".Hobby" />
        <activity android:name=".Weapon" />
        <activity android:name=".Villain" />
        <activity android:name=".Reward" />
        <activity android:name=".Story" />
    </application>

</manifest>